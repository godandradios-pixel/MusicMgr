venv/
data/
__pycache__/
*.pyc
*.pyo
.pytest_cache/
build/
dist/
*.spec.bak

library.db

# Generated by build.ps1 right before packaging - see version.py's own
# docstring for why this is deliberately not committed (it names whatever
# commit was checked out at build time, which goes stale the moment
# another commit lands).
musicmgr/assets/VERSION

.commitmsg
.review.diff
Claude outputs/
last_fm.txt
