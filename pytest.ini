[pytest]
# Makes "musicmgr" importable no matter how pytest is invoked (bare `pytest`,
# `python -m pytest`, or from an IDE) - `.` is this file's own folder, i.e.
# the repo root that `run.py` and `musicmgr/` both live in.
pythonpath = .
testpaths = tests
